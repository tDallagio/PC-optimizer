CONTEXTO
========
La documentacion del proyecto quedo atrasada respecto al codigo. Durante el epic del
auto-updater v4.1 (Camino B) se hicieron varios cambios de codigo que nunca se
registraron, y CLAUDE.md tiene datos viejos y una regla contradictoria que rompe el
flujo de trabajo. Hay que sincronizar y reparar el flujo para que no se repita.

Son tres tareas. La 1 y la 3 reparan el flujo; la 2 registra el trabajo perdido.


TAREA 0 - Ubicar los archivos de docs (hacer primero)
=====================================================
CLAUDE.md referencia "docs/PENDIENTES.md" y "docs/CHANGELOG.md", pero pueden estar
en la raiz del proyecto. Antes de tocar nada, ubicar la ruta REAL de PENDIENTES.md
y CHANGELOG.md (buscar en raiz y en docs/). Usar esa ruta real en las tareas
siguientes y reportarmela.


TAREA 1 - Reparar y actualizar CLAUDE.md
========================================
1. Titulo (linea 1): "# WinBoost v4.0" -> "# WinBoost v4.1".

2. Conteos de lineas (en la seccion "Archivos del proyecto"):
   - OptimizarPC_App.ps1: el texto dice "7100+ lineas". El archivo tiene ~10400.
     Actualizar a "10400+ lineas".
   - OptimizarPC_UI.xaml: dice "2100+ lineas". Tiene ~2550. Actualizar a "2550+ lineas".

3. Orden de tabs: la lista va de 0 a 8 pero falta el tab 9. Agregar:
   "- 9 = Tuning Avanzado"
   Y en la seccion de patrones, donde dice "Navegacion sidebar: Set-ActiveNav [0-8]",
   cambiar a "Set-ActiveNav [0-9]".

4. REGLA CONTRADICTORIA (importante para el flujo): en la seccion "Reglas — SIEMPRE"
   hay una linea que dice "Al modificar un archivo, devolver el archivo completo".
   Eso contradice la convencion real de trabajo. Reemplazarla por:
   "Al modificar un archivo, guardar los cambios directamente en su ruta. No imprimir
   el contenido del archivo en el chat."

5. REGLA NUEVA DE LOGGING (la causa raiz del desfasaje): en "Reglas — SIEMPRE",
   agregar:
   "TODO cambio de codigo (feature, fix o mantenimiento) se registra en CHANGELOG.md,
   no solo los items del roadmap (F-numerados). Un cambio sin entrada en CHANGELOG.md
   esta incompleto."

6. Rutas de docs: si en la TAREA 0 confirmaste que PENDIENTES.md y CHANGELOG.md estan
   en la RAIZ (no en docs/), corregir todas las referencias "docs/PENDIENTES.md" y
   "docs/CHANGELOG.md" de CLAUDE.md para que apunten a la ruta real. Si efectivamente
   estan en docs/, dejar como esta.


TAREA 2 - Agregar la entrada del auto-updater v4.1 al CHANGELOG.md
=================================================================
Insertar esta entrada al inicio del historial (donde van las mas recientes), con el
mismo estilo de las demas entradas:

---

## Auto-updater v4.1 — Camino B (instalador silencioso) + verificacion de integridad

**Archivos modificados**
- `OptimizarPC_App.ps1` — `$UPDATE_CHECK_URL`, `Start-UpdateDownload`, `Apply-Update`,
  inyeccion de version en la GUI
- `OptimizarPC_UI.xaml` — Title de la ventana, `lblVersion`, `lblVersionAbout`
- `installer/WinBoost.iss` — seccion [Run] y [Setup]
- `version.json` — version, downloadUrl, sha256

**Repo y URLs**
- `$UPDATE_CHECK_URL` y `releaseUrl` apuntaban al repo viejo `OptimizarPC`; corregidos
  a `PC-optimizer` (el repo real del release).
- `downloadUrl` apuntaba a la pagina del tag (`/releases/tag/v4.0`), que devuelve HTML,
  no el binario; corregido al asset directo (`/releases/download/v4.1/WinBoost_Setup_4.1.exe`).

**Modelo de actualizacion: portable -> instalador**
- El modelo anterior copiaba un exe portable encima del exe en ejecucion, incompatible
  con la distribucion por instalador Inno Setup.
- `Apply-Update` reescrito: un helper (`do_update.ps1` en `%TEMP%\OptimizarPC_update`)
  espera a que cierre el proceso, ejecuta el instalador con
  `/VERYSILENT /SUPPRESSMSGBOXES /NORESTART /NOCANCEL`, chequea ExitCode y relanza la app.
  El instalador actualiza exe + XAML + accesos directos de forma atomica (mata el bug de
  XAML desincronizado del modelo portable). El instalador hereda la elevacion del proceso
  (sin segundo UAC).
- Guard de modo desarrollo: si no corre como exe compilado, no ejecuta el instalador;
  abre la pagina del release en el navegador.

**Verificacion de integridad (hardening)**
- Se elimino el bypass donde un `sha256` vacio salteaba la verificacion (`if ($expectedHash)`):
  ahora un hash ausente ABORTA la instalacion en vez de saltearla.
- `Start-UpdateDownload` valida que el archivo descargado exista (Test-Path) y calcula el
  hash en try/catch. Ante archivo ausente/ilegible (cuarentena de antivirus) o mismatch de
  hash, no instala y avisa; en el caso de cuarentena hace fallback a la pagina del release.

**Fix error 740 en el instalador (`WinBoost.iss`)**
- La entrada [Run] postinstall lanzaba `WinBoost.exe` (manifest requireAdmin) via
  CreateProcess, que no puede elevar -> error 740. Solucion: flags `shellexec`
  (usa ShellExecute, respeta la elevacion) y `skipifsilent` (no autolanzar en el update
  silencioso, donde el relanzamiento lo hace el helper).
- [Setup]: `CloseApplications=yes`, `RestartApplications=no`, `UsePreviousAppDir=yes`.

**Version en la GUI derivada de `$VERSION`**
- El Title de la ventana y los labels `lblVersion` (sidebar) y `lblVersionAbout` (About)
  tenian "v4.0" hardcodeado en el XAML. Se vaciaron en el XAML y ahora se inyectan en
  runtime desde `$VERSION` en `Add_ContentRendered` (y `lblVersionAbout` tambien en
  `Render-SettingsUI`, que es lazy). Fuente unica de version: `$VERSION`.

**version.json**
- `version: "4.1"`, `downloadUrl` al asset v4.1, `sha256` real del instalador.

---


TAREA 3 - Nota de correccion en PENDIENTES.md (no destructiva)
==============================================================
El item F2.10 menciona "Apply-Update sin notificacion si Copy-Item falla", que describe
el modelo portable ya reemplazado. NO borrar el item (es historico de la auditoria).
Agregar al final de la linea de ese punto (a) una nota entre parentesis:
"(actualizado v4.1: el modelo Copy-Item fue reemplazado por instalador silencioso; ver
CHANGELOG)".


VERIFICACION
============
- Confirmar que CLAUDE.md ya no contiene "devolver el archivo completo" ni "v4.0" en el
  titulo, y que incluye el tab 9 y la regla nueva de logging.
- Confirmar que CHANGELOG.md tiene la entrada nueva del auto-updater.
- Reportarme un resumen corto de los cambios (no volcar los archivos completos).

Guardar los cambios directamente en CLAUDE.md, CHANGELOG.md y PENDIENTES.md (en sus
rutas reales). No imprimir el contenido de los archivos en el chat.
